# Core
sqlalchemy>=2.0.0
pydantic>=2.0.0
pyyaml>=6.0

# Vector search (fallback only)
faiss-cpu>=1.7.4
numpy>=1.24.0

# LLM clients (user configures one)
openai>=1.0.0
anthropic>=0.30.0

# Temporal
python-dateutil>=2.8.0
pytz>=2023.3

# Tokenization (for budget enforcement)
tiktoken>=0.5.0

# Demo & evaluation
rich>=13.0.0
tabulate>=0.9.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
